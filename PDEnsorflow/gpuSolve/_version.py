"""Single source of truth for the package version.

Bump the version *here* (and the title line in the repo README, which is
Markdown and cannot import Python). Every package ``__init__`` re-exports
``__version__`` from this module, and ``setup.py`` parses it, so there is
exactly one number to change.
"""

__version__ = ['1', '3', '2']
